var assert = require('assert');


suite('maps', function() {
  test('test existing t9n', function(done, server) {
  
    server.eval(function() {
      name = T9n.get('name');
      console.log ('name: ' + name);
      emit('name', name);
    });

  
    server.once('name', function(name) {
      console.log ('hi: ' + name);
//      name2 = T9n.get('name');
      assert.equal(name, 'Name');
      done();
      //emit('docs', docs);
    });

//    server.once('docs', function(docs) {
//      assert.equal(docs.length, 1);
//      done();
//    });
  });
});

